[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
numReplicas = 1
startCommand = "node server.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

# Force Railway to use Dockerfile and not Nixpacks
[build.env]
NIXPACKS_NO_CACHE = "1"
